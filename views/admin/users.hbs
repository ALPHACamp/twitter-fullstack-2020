{{>admin-left route='usersList'}}
<div class="col-12 mt-3" style="height:1200px;width:980px;">
  {{>admin-header route='usersList'}}
  <div class="row row-cols-3">
    {{#each users}}
      {{> admin-user}}
    {{/each}}
  </div>
</div>