<div class="d-flex flex-column justify-content-between">
  {{>alphitter}}
  <h4 class="text-center fw-bold my-4">後台登入</h4>

  {{>messages}}

  <form action="/admin/signin" method="post" class="d-flex flex-column">
    {{>signin-form-control}}
  </form>

  <div class="d-flex flex-row justify-content-end">
    <a href="/signin" class="link-primary">前台登入</a>
  </div>

</div>