{{>left route='profile'}}
<div class="col-6">
  <div class=" card ">
    {{>user-header}}
    {{>users-info}}
    {{>users-tab route='tweets'}}
    {{#each tweets}}
      <div>
        {{>tweet route='tweets'}}
      </div>
    {{/each}}
  </div>
</div>
{{>right}}