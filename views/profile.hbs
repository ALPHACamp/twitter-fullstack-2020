{{>left route='profile'}}
<div class="col-6">
  <div class="card">
    {{>user-header route='profile'}}
    {{>users-info route='profile'}}
    {{>users-tab route='tweets'}}
  </div>
  {{#each tweets}}
  <div>
    {{>tweet route='personal-tweets'}}
  </div>
  {{/each}}
</div>
{{>right}}