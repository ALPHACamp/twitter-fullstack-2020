{{>left route='profile'}}
<div class="col-6">
  <div class="col container">
    <div class="content" style="border:1px solid #E6ECF0">
      {{>user-header route='profile'}}
      {{>users-info}}
      {{>users-tab route='reply'}}
    </div>
    <div class="profile-reply">
      {{#each replies}}
      {{>tweet route='personal-reply'}}
      {{/each}}
    </div>
  </div>
</div>
{{>right}}