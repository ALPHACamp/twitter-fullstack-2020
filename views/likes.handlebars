<div class="d-block justify-content-center" style="margin-left: 17rem;">
  <div class="contener" style="width: 30rem; height:3rem; border-top: thin solid #e6ecf0;">
    <div class="d-flex justify-content-start">
      <div class="fs-2 mx-2 pt-2">
        <i class="bi bi-arrow-left-short"></i>
      </div>
      <div class="">
        <p class="my-0" style="font-weight: bold;">{{user.name}}</p>
        <small>{{user.likeTweetsCount}} 推文</small>
      </div>

    </div>
  </div>

  <!--User 頭像&背景-->
  <div class="contener" style="width: 30rem; height:12rem; border-top: thin solid #e6ecf0;">
    <div class="user-profile-card d-flex flex-column justify-content-start text-center">
      {{> message}}
      <img class="user-profile-cover" src="{{user.cover}}" alt="no pic">
      <img class="user-profile-avatar" src="{{user.avatar}}" alt="no pic">
      <div class="d-flex justify-content-end m-1">
        <a href="/users/{{user.id}}/edit"><button class="btn user-profile-btn">編輯個人資訊</button></a>
      </div>
    </div>
  </div>

  <!--User 基本資料-->
  <div class="contener" style="width: 30rem; height:10rem; border-top: thin solid #e6ecf0;">
    <div class="mx-2 pt-2">
      <p class="m-0" style="font-weight: bold;">{{user.name}}</p>
      <small>@{{user.account}}</small>
      <div class="py-2">
        <p>{{user.introduction}}</p>
      </div>
      <div>
        <span class="mr-2">{{user.followingsCount}} 個追蹤中</span>
        <span>{{user.followersCount}} 位追蹤者</span>
      </div>
    </div>
  </div>

  <!--Navs-->
  <div class="contener" style="width: 30rem; height:3rem; border-top: thin solid #e6ecf0;">
    <ul class="nav justify-content">
      <li class="nav-item">
        <a href="/users/{{user.id}}" class="nav-link" style="color:black;">推文</a>
      </li>
      <li class="nav-item">
        <a href="/users/{{user.id}}/repliesList" class="nav-link" style="color:black;">推文與回覆</a>
      </li>
      <li class="nav-item">
        <a href="/users/{{user.id}}/likes" class="nav-link active router-link-active" aria-current="page"
          style="color:orangered;border-bottom-style: solid; border-width: 1px; border-color:orangered;">喜歡的內容</a>
      </li>
    </ul>
  </div>

  <!--Tweets-->
  {{#each likeTweets}}
  <div class="contener"
    style="width: 30rem; height:11rem; border-top: thin solid #e6ecf0; border-right: thin solid #e6ecf0;">
    <div class="card-body">
      <div class="row">

        <div class="col-2">
          <a href="/users/{{this.id}}">
            <img class="user-profile-tweets-avatar" src="{{this.avatar}}" alt="no pic">
          </a>
        </div>
        <div class="col-10">
          <h5 class="card-title" style="display: inline-block;">{{this.name}}</h5>
          <a href="{{this.id}}" class="card-link" style="display: inline-block;">@{{this.account}}</a>
          <p class="card-text" style="display: inline-block;"><small class="text-muted">{{this.elapsedTime}}</small>
          </p>
          <p class="card-text">{{this.description}}</p>
        </div>

        <div style="position: absolute; margin-top: 6.5rem; margin-left: 4.5rem;">
          <button type="button" class="bi bi-chat reply_btn"><span class="ml-2">{{this.repliesCount}}</span></button>
          <button type="button" class="bi bi-heart like_btn"><span class="ml-2">{{this.likesCount}}</span></button>
        </div>

      </div>
    </div>
  </div>
  {{/each}}
</div>