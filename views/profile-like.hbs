{{>left route='profile'}}
<div class="col-6">
 <div class="col container">
  <div class=" card mb-3">
   {{>user-header route='profile'}}
   {{>users-info}}
   {{>users-tab route='likes'}}
   <div class="">
    {{#each tweets}}
     {{>tweet route='personal-likes'}}
    {{/each}}
   </div>
  </div>
 </div>
</div>
{{>right}}