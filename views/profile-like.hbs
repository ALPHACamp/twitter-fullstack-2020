{{>left route='profile'}}
<div class="col-6">
  <div class="col container">
    <div class="content" style="border:1px solid #E6ECF0">
      {{>user-header}}
      {{>users-info}}
      {{>users-tab route='likes'}}
      <div class="profile-like">
        {{#each tweets}}
        {{>tweet route='tweets'}}
        {{/each}}
      </div>
    </div>
  </div>
</div>
{{>right}}