<div class="tweets-container">
  {{#each follows}}
  <div class="tweets d-flex mt-3" style="border-bottom: 1px solid #C4C4C4;">
    <div class="avatar ps-3">
      <a href="/users/{{this.id}}/tweets" style="text-decoration:none;">
        <img src="{{this.avatar}}" class="rounded-circle me-3"
          style="width: 50px;height: 50px;background: #C4C4C4;">
      </a>
    </div>
    <div class="tweet-content">
      <div class="name-container d-flex justify-content-between pe-3" style="width: 555px;">
        <div class="name">
          <a class="user-text-link" href="/users/{{this.id}}/tweets">
          <span style="font-size: 16px;font-weight:700;">{{this.name}}</span>
          </a>
        </div>
        {{#if isnotUser}}
        {{#if this.isFollowed }}
          <form action="/followships/{{this.id}}?_method=DELETE" method="POST">
            <button type="submit" class="follow-btn-active" value="{{this.id}}" name="id">正在跟隨</button>
          </form>
        {{else}}
          <form action="/followships" method="POST">
            <button type="submit" class="follow-btn" value="{{this.id}}" name="id">跟隨</button>
          </form>
        {{/if}}
        {{/if}}
      </div>
      <div class="content mt-1 mb-3 ms-0">
        {{this.introduction}}
      </div>
    </div>
  </div>
  {{/each}}
</div>