<div class="tweet-card">
  <a href="/users/{{this.User.id}}/tweets">
    <img class="me-auto rounded-circle" src="{{this.User.avatar}}" alt="avatar">
  </a>
  <a class="tweet-card-name" href="/users/{{this.User.id}}/tweets">
    <span class="font-name">{{this.User.name}}</span>
    <span class="font-account">@{{this.User.account}}・{{relativeTimeFromNow this.createdAt}}</span>
  </a>
  <div class="text-box">
    <span>{{this.description}}
      <form action="/tweets/{{this.id}}/replies" method="GET">
        <div class="speech-bubble">
          <button type="submit">
            <a href="" data-bs-toggle="modal" data-bs-target="#replyInputModatl{{this.id}}">
              <img src="https://upload.cc/i1/2022/07/26/rKCiDo.png" alt="reply">
            </a>
          </button>
          <div class="font-account">50</div>
        </div>
      </form>

      {{#if this.Likes.id}}
      <form action="/tweets/{{this.id}}/unlike" method="POST">
        <div class="like-w">
          <button type="submit">
            <img src="images/icons/like.png" alt="like">
          </button>
          <div class="font-account">50</div>
        </div>
      </form>
      {{else}}
      <form action="/tweets/{{this.id}}/like" method="POST">
        <div class="like-w">
          <button type="submit">
            <img src="https://upload.cc/i1/2022/07/26/ZdBEx9.png" alt="like">
          </button>
          <div class="font-account">50</div>
        </div>
      </form>
      {{/if}}

    </span>
  </div>
</div>