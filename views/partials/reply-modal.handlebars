<div class="modal fade" id="replyModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content reply-modal">
      <div class="modal-header">
        <button type="button" class="btn btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <div class="card reply-tweet-lg">
          <a href="/tweets/{{this.Tweet.id}}/replies" class="tweet-href"></a>
          <div class="reply-avatar">
            <div class="avatar">
              <a href="/users/{{this.UserId}}/tweets" class="primary-anchor">
                {{#if this.User.avatar}}
                <img src="{{ this.User.avatar }}" alt="avatar" class="logo">
                {{else}}
                <img src="/stylesheets/icons/avatar.png" alt="avatar" class="logo">
                {{/if}}
              </a>
            </div>
            <div class="reply-line">
              <div class="vertical-line"></div>
            </div>
          </div>
          <div class="card-text">
            <div class="card-text-info">
              <a href="/users/{{ this.UserId }}/tweets" class="primary-anchor">
                <span class="card-name">Apple</span>
              </a>
              <a href="/users/{{ this.UserId }}/tweets" class="primary-anchor">
                <span class="card-account">@apple</span>
              </a>
              <span class="card-dot">·</span>
              <span class="card-datetime">3小時前</span>
            </div>
            <p class="card-description">Ab amet fugit. Nam et iusto dolore nemo voluptatum aut. Rem quisquam autem nihil odit
              perferendis do</p>
            <div class="reply-to-part">
              <span class="reply-to">回覆</span>
              <a href="/users/{{ this.Tweet.User.id }}/tweets" class="primary-anchor">
                <span class="card-account reply-account">@apple</span>
              </a>
            </div>
          </div>
        </div>
        <div class="main-create-bar">
          <div class="avatar">
            {{#if user.avatar }}
            <img src="{{ user.avatar }}" alt="avatar" class="logo">
            {{else}}
            <img src="/stylesheets/icons/avatar.png" alt="avatar" class="logo">
            {{/if}}
          </div>
          <form action="/tweets" method="POST" class="create-reply-form reply-content">
            <textarea name="reply" id="reply" cols="30" rows="10" class="reply-create-input"
              placeholder="推你的回覆"></textarea>
        </div>
      </div>
      <div class="modal-footer">
        <div>
          <h6 class="tweetTips">內容不可以空白</h6>
        </div>
          <button type="submit" class="btn btn-info">回覆</button>
        </form>
      </div>
    </div>
  </div>
</div>