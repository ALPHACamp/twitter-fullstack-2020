<section id="Huei-region">
  <div class="container nav-user">
    <h4 class="nav-user-title">推薦跟隨</h4>
    <ul>
      <p class="list-separate"></p>
      {{#each users}}
      <li>
        <div class="d-flex align-items-center user-list mt-4">
          <a href="/users/{{this.id}}/tweets">
            <div class="d-flex">
              <img src="{{#if this.avatar}}{{this.avatar}}{{else}}/images/alphacamp.png{{/if}}" alt="" class="circle">
              <div class="d-flex flex-column user-name-position">
                <p class="text-dark">{{this.name}}</p>
                <p class="text-black-50">@{{this.account}}</p>
              </div>
              {{#ifCond this.id ../user.id}}
              {{else}}
              {{#if this.isFollowed}}
              <form action="/followships/{{this.id}}?_method=DELETE" method="POST">
                <button type="submit" class="btn btn-white rounded-pill mt-1 user-follow-bottom" name="id"
                  value={{this.id}}>
                  <span class="p">追隨</span>
                </button>
              </form>
              {{else}}
              <form action="/followships" method="POST">
                <button type="submit" class="btn btn-orange rounded-pill mt-1 user-follow-bottom" name="id"
                  value={{this.id}}>
                  <span class="">正在追隨</span>
                </button>
              </form>
              {{/if}}
              {{/ifCond}}
            </div>
          </a>
        </div>
      </li>
      {{/each}}
    </ul>
  </div>
</section>